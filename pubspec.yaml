name: signspeak
description: A sign language app for deaf community
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
<<<<<<< HEAD

  # Firebase
=======
  
  # ===== CAMERA =====
  camera: ^0.10.6

  # ============================================
  # ✅ TENSORFLOW LITE (UPDATED VERSION)
  # ============================================
  tflite_flutter: ^0.11.0
  
  # ============================================
  # ✅ IMAGE PROCESSING (UPDATED VERSION)
  # ============================================
  image: ^4.2.0
  
  # ============================================
  # ✅ HTTP (UPDATED VERSION)
  # ============================================
  http: ^1.3.0
  
  # ===== PERMISSIONS =====
  permission_handler: ^11.3.1
  
  # ===== UI & UTILITIES =====
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.4
  cached_network_image: ^3.4.1
  flutter_svg: ^2.3.0
  google_fonts: ^6.3.3


  
  # ===== STATE MANAGEMENT =====
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  
  # ===== NAVIGATION =====
  go_router: ^14.8.1
  
  # ===== FIREBASE =====
>>>>>>> 35e970310cb11d666f04aaeee5c1d9fd66ccb810
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  google_sign_in: ^6.3.0
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.4.10

  # Storage
  shared_preferences: ^2.5.0

  # UI
  cupertino_icons: ^1.0.9
  google_fonts: ^6.3.3
  flutter_svg: ^2.3.0

  # Camera
  camera: ^0.10.6
  image_picker: ^0.8.9
  permission_handler: ^11.3.1

  # Navigation
  provider: ^6.1.5+1
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/models/alif_robust.tflite
    - assets/models/hand_landmarker.tflite  
    - assets/models/hand_detection.tflite